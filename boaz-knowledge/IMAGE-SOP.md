# 📐 BOAZ 产品图片 SOP v2.0

> 适用所有新产品。发给我的方式和后续处理标准。

---

## 0. 你发图 → 我处理 → 你确认

### 你需要做什么

提供以下信息（发飞书文档/直接聊天均可）：

| 信息 | 说明 | 必须？ |
|------|------|--------|
| **产品ID** | 英文唯一标识，如 `240g-vintage-crop` | ✅ |
| **产品名称** | 如 `240gsm Vintage Washed Cropped T-Shirt` | ✅ |
| **颜色列表** | 每个颜色的名称 + 十六进制色值 | ✅ |
| **4类图片** | SKU颜色图 / 主图框 / 正背面图 / 尺码图 | ✅ |
| **尺码数据** | 每个尺码对应的胸围/衣长/肩宽（cm）| 建议提供 |
| **价格/FOB** | 产品单价 | 建议提供 |

### 怎么发图片

1. **直接聊天发图片/压缩包** — 最直接
2. **飞书文档表格贴图片链接** — 我能读文档处理
3. **GitHub `/raw-images/` 目录** — push 上来

### 我处理完会做什么

1. ✅ 4类图片分类、resize、转 WebP
2. ✅ 更新 `products-catalog.ts`（路径 + Alt 文本）
3. ✅ 更新定制页颜色列表
4. ✅ 构建部署到 boazclothes.com
5. **通知你确认** — 你去网站上看效果
6. 如果图片要替换，给新链接/新图，我重走流程

---

## 1. 目录结构

每个产品一个文件夹：
```
public/images/products/<产品id>/
├── sku/                    # SKU图（白底平铺，每色2张：正+背）
│   ├── sku-<颜色名>.webp          # 正面
│   └── sku-<颜色名>-back.webp    # 背面（定制页正反面切换用）
├── model/                  # 主图框（模特图 + 细节图）
│   ├── model-1.webp       # 模特上身图
│   ├── model-2.webp
│   ├── detail-1.webp      # 细节特写（面料/领口/做工）
│   ├── detail-2.webp
│   └── ...
├── size-chart/             # 尺码图
│   └── size-chart.webp
└── raw/                    # 原始备份（不改原图）
```

## 2. 你只需发 4 类原图

| 类型 | 数量 | 说明 |
|------|------|------|
| **SKU颜色图** | 每色 **1 张** | 白底正面平铺，突出颜色 |
| **正背面图** | 每色 **2 张**（正面+背面） | 定制页展示正反面用 |
| **主图框** | 模特图 2-5张 + 细节图 2-3张 | 模特上身展示版型 + 面料/领口/做工特写，背景干净无字 |
| **尺码表** | 1 张 | 工厂原始尺码表（有中文我来翻） |

> 发原图。resize、webp 转换、翻译我都包了，不需要你自己处理。

### 主图框排序优先级

我收到主图框后按以下顺序排列 Gallery：

**人物图 > 挂拍图 > 衣服整体图 > 衣服局部图**

| 优先级 | 类型 | 说明 |
|--------|------|------|
| 🥇 高 | 人物图 | 模特上身实拍，展示版型 |
| 🥈 中 | 挂拍图 | 挂拍展示整体外观 |
| 🥉 中 | 衣服整体图 | 平铺展示整体 |
| 📍 低 | 衣服局部图 | 面料/领口/做工细节特写 |

> 尺码图始终放在 Gallery 最后一位。

## 3. 命名规则（我来做）

| 类型 | 命名格式 | 例 |
|------|---------|-----|
| SKU正面 | `sku-<颜色>.webp` | `sku-white.webp` |
| SKU背面 | `sku-<颜色>-back.webp` | `sku-white-back.webp` |
| 模特图 | `model-<序号>.webp` | `model-1.webp` |
| 尺码表 | `size-chart.webp` | `size-chart.webp` |

> 你不用管命名，发原图来我统一处理。

## 4. 格式标准

| 参数 | 标准 |
|------|------|
| 格式 | **WebP** |
| 品质 | 82% |
| SKU图 | 800×800px（方形） |
| 模特图 | 1536×2752px（竖版，不补白） |
| 尺码表 | 800×600px |

## 5. 缺图处理

- 正背面图缺某色背面 → **先上空位，后续补**，定制页该色 Back 按钮灰显
- 主图框不足 → 有多少上多少
- 尺码表必填，没有就上不了线

## 6. 我处理流程

```
收到图片 → 存 raw/ 备份 → 分类 → resize → 转 WebP → 
更新 products-catalog.ts → 更新定制页 → 构建 → 部署 → 通知你
```
