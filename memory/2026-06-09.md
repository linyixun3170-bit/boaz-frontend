# 2026-06-09

## 价格隐藏 + 部署上线
- 创建 `lib/config.ts`，内置 `SITE_CONFIG.showPricing = false` 全局开关
- 隐藏价格位置：
  - 产品列表页 → "Contact for Pricing"
  - 产品详情页 → 金色 "Contact for Pricing"
  - 定制页 → 隐藏产品价格、阶梯价、价格汇总、移动端价格条
  - Hero → 隐藏 "$1.99 blanks to $9.90"
  - TwoWays → 隐藏 "$1.99 Starting" → "In Stock"
  - FAQ → 价格区间回答切换为无价格版本
- 部署方式：`npx wrangler pages deploy out/` 推送到 Cloudflare Pages
- 注意：Cloudflare Pages 项目未绑定 GitHub 自动部署，需要手动 wrangler deploy

## 恢复价格
改 `lib/config.ts` 中 `showPricing: false` → `true`，然后重新构建 + wrangler deploy

## SEO 修复
- robots.ts: `disallow: "/"` → `allow: "/"` + sitemap 引用
- layout.tsx: robots meta → `index, follow` ✅ Google 可收录
- sitemap.ts: 从空数组改为包含所有产品页 + 静态页面
- layout.tsx meta description: 去掉价格数字

## Schema.org 动态化
- 去掉 "use client"，改为服务端组件（确保静态 HTML 输出）
- URL 修复：boaz.apparel → boaz-clothes.com
- product 类型从硬编码示例改为 products-catalog.ts 动态生成
- 每个产品详情页有独立 Product + Offer Schema
- 组织：Organization + WebSite + WebPage 三层结构

## 表单可靠性
- 前端添加 AbortController，8 秒超时提醒 + WhatsApp 兜底
- API 端 contact.js 添加 sendFallbackEmail()
- 飞书失败时自动发邮件（需配 SENDGRID_API_KEY）

## 域名迁移 boazclothes.com → boaz-clothes.com
- Cloudflare Pages 添加新域名 `boaz-clothes.com` + `www.boaz-clothes.com`
- DNS 删除 A 记录，创建 CNAME → `boaz.pages.dev`
- 全站代码 URL 替换：boazclothes.com → boaz-clothes.com（15+ 文件）
- 邮箱全量替换：hello@boazclothes.com / info@boazclothes.com → sale@boaz-clothes.com
- Cloudflare Pages 环境变量 EMAIL_FALLBACK_TO = sale@boaz-clothes.com
- 旧 boaz.apparel 引用修复（Instagram 句柄保持 boaz.apparel）
- 域名状态：✅ active（SSL 已签发）

## 页面新增
- `/privacy-policy` — Privacy Policy 法律页面
- `/terms-and-conditions` — Terms & Conditions 法律页面
- Footer 底部新增 Privacy / Terms 链接
- sitemap.xml 包含新页面

## Bug 修复
- 首页 Products 组件第一个产品图 XJ-78000 路径缺少 `model/`，已修复

## 多语言默认英文
- lib/i18n/context.tsx 去掉浏览器语言自动检测逻辑
- 默认永远显示英文，用户手动选择后才切换并记住

## GA4 配置
- Google Analytics 4 账号创建完成
- Measurement ID: **G-4L2NLJ75Z2**
- 已配置到 components/Analytics.tsx
- 已部署生效（HTML 确认加载 gtag + googletagmanager）

## 技能安装
- ✅ marketing-skills v0.1.2
- ✅ gingiris-seo-geo-agent v1.0.8（替代 seo-ops）
- ✅ ai-growth-engine v1.0.0（替代 growth-engine）
- 📦 afrexai-growth-engine v1.1.0（额外安装，未激活）
